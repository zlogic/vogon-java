Vogon Web version deployment instructions for ${readme.server.name}

Runs on OpenShift's free tier!
Just create a ${readme.openshift.catridge_name} container and deploy as ROOT.war.
See instructions at http://help.openshift.com/hc/en-us/articles/202399740

Default username/password are Default/Default.

IMPORTANT:
Change the username/password, as the default user is the administrator!
