Vogon Web version deployment instructions for ${readme.server.name}

Runs on Heroku's free tier!
Just clone the Git repository into a new Heroku app.

Default username/password are Default/Default.

IMPORTANT:
Change the username/password, as the default user is the administrator!

This application can run in standalone mode, but will redirect to HTTPS by
default. The keystore file path needs to be provided via the
"vogon.keystore.file" Java option and its password needs to be provided via the 
"vogon.keystore.pass" Java option.